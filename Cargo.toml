[workspace]
resolver = "2"
members = ["core"]
# "desktop" and "web" are added once their crates exist (Phases 5).

[workspace.package]
edition = "2021"
license = "MIT"
authors = ["momin"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
